# Context Summary

Persistent record of architectural decisions, discovered patterns, gotchas, and active context.
This file is referenced in CLAUDE.md and loaded every session.

## Active Context
- **Multi-playlist sync planning complete (2026-05-07).** Exploration `.harness/explorations/multi-playlist-sync.md` + plan `.harness/explorations/multi-playlist-sync-IMPLEMENTATION-PLAN.md` cover four new features: F-016 (config registry), F-017 (multi-playlist fetch + membership), F-018 (multi-playlist write), F-016b (orchestrator wiring). Decisions locked: Q1 unify Liked Songs into playlist_membership; Q3 MAX_PLAYLISTS_PER_RUN=3; Q4 backfill pre-sets synced_at=matched_at for currently-matched Liked tracks; Q-num multi-playlist owns F-016/F-017/F-018 (bidirectional renumbers if it ships); Q-prio functional order — F-009 M5 error attribution ships before F-016b touches the orchestrator.
- **Next session task list (12 entries set up via TaskCreate):** locking decisions ✓, features.json updates ✓, context_summary refresh (this), then F-009 M5 fix → F-016 Phase A (spec → schema.sql → DB layer → provider helper → seeder → env vars → coverage gate) → CHECKPOINT for live Neon migration (held until Ovidiu present at terminal). Then Phase B Agent Team for F-017+F-018, Phase C single-session F-016b, Phase D production deploy with the F-015 playbook.
- **Bidirectional sync exploration (`.harness/explorations/bidirectional-sync.md`)** is parallel and orthogonal per its §122. Schema designs do NOT collide (multi-playlist adds playlist_configs + playlist_membership; bidirectional adds tidal_extras + sync_runs.reconcile_removed). Feature numbers DO collide — bidirectional renumbers to F-019+ if/when it ships.
- **Untracked dirs (.claude/commands/opsx/, .claude/skills/openspec-*/, openspec/)** — Ovidiu confirmed leave-alone. These are personal opsx tooling; not committed, not gitignored at project level.
- **Sprint 7 minor follow-ups still open** (interleave during multi-playlist work, none block it): F-012 M1 (candidates fetch returns []), F-012 M2/M3 (R10/R11 fuzzy filter — skipped + within-7-day-pending exclusion), F-013 M4 (R9 orphan ISRC UNION in fetchNewTracks), M7 (HTTP route logging swallows). F-009 M5 (discriminated error_code) is no longer "minor" — promoted to a hard prereq for F-016b.
- **Production state stable** (Sprint 7 deploy + F-015 + F-009 partial-run diagnostics). Cron at 2x/day (commits 0bfa718 + 2a54e93). 22 features tracked; 18 passing, 4 pending (the multi-playlist set).
- **Sprint 6 close-out (preserved for reference):** project-level reviewer verdict was SHIP-WITH-FOLLOW-UPS. Full review in `.harness/reviews/sprint-5-final.md`.
- Test totals: 462 unit + 18 integration (was 8 — F-005/F-008/F-009 paths added) + 8 e2e (was 6 — T-009-10 wall-time + T-011-13 p95 added). All gates green.
- Sprint 6 harness improvements landed: Stage 4 extended to also gate spec-declared `CREATE INDEX idx_*` references against db/schema.sql; new Stage 5 enforces TODO(ovidiu) markers above api.spotify.com/openapi.tidal.com URL constants. TaskCompleted hook now has 5 mechanical stages: smoke → full_test → coverage-claim → schema-drift (columns + indices) → TODO markers.
- Live Neon migrations: now in sync. db/schema.sql includes idx_matches_tidal + idx_captures_spotify_id (were on Neon, missing from file — fixed in `a07f554`).
- Sprint 5 close-out details (preserved below): F-011 + F-009 + F-010 + F-012 + F-013 shipped; live Neon migrations applied; Stage 4 schema-drift gate landed.
- Live Neon migrations applied via MCP this session: `error_code TEXT` column on `sync_runs` (F-009), `idx_captures_spotify_id` on `captures` (F-013).
- One real coordination failure + one false-positive misread by the lead: (a) Wave A orchestrator proceeded without applying the lead's plan_approval revision on the advisory lock — lead applied Pool/WebSocket fix in `7012a39`. (b) Wave B+C captures teammate appeared to idle without acking the follow-up; lead started parallel work, then captures' SendMessage arrived ~5 minutes late confirming they had picked up the message and done the work correctly (commit 9705b4b). Lesson: SendMessage delivery latency can be on the order of minutes; lead should give a 5-min grace window + check git log before assuming non-response. The "silent compliance" framing originally applied to this case was wrong — they were just slow to deliver, not non-compliant.
- Stage 4 schema-drift hook landed (commit 34f28c0): `.claude/hooks/check-schema-drift.py` greps src/ for SQL column references missing from db/schema.sql; verify-task-quality.sh integrates it as a hard reject. Tested against simulated Sprint 4 regression — catches it. No false positives on current code.
- Sprint 6 carry-overs: plan-approval compliance enforcement; per-feature commit hygiene (F-010 cron commit bundled F-013's index.ts edit); Stage 5 TODO-marker hook; explicit teammate ack required before lead assumes follow-up not started.
- Specs authoritative; F-005 amended for nullability (Sprint 3); F-008 PLAYLIST_PRIVACY constant + 2 Tidal endpoint TODOs flagged for Ovidiu's verification (Sprint 4); F-009 added `error_code` column to sync_runs (extension, not amendment — the spec already implied it via the failure-modes table)
- Mode: Agent Teams, max-parallelization
- Worker domain (production): `portage.eovidiu.co.uk` (architecture spec's `sync.example.com` was placeholder)
- Tidal playlist title (target): `Spotify Liked` (from `.env` `TIDAL_PLAYLIST_TITLE`)
- Tidal country code: `RO` (from `.env` `TIDAL_COUNTRY_CODE`)

## Available Credentials (names only — values in `.env`, never committed)
- **Spotify**: `SPOTIFY_CLIENT_ID`, `SPOTIFY_CLIENT_SECRET`
- **Tidal**: `TIDAL_CLIENT_ID`, `TIDAL_CLIENT_SECRET`, `TIDAL_COUNTRY_CODE`, `TIDAL_PLAYLIST_TITLE`
- **Cloudflare**: `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_ZONE_ID`, `CLOUDFLARE_API_TOKEN`, `WORKER_DOMAIN`
- **Neon**: `DATABASE_URL`, `NEON_API_KEY`
- **Worker runtime secrets** (to be minted, not in `.env`): `JWT_SECRET` (32+ bytes), `TOKEN_ENCRYPTION_KEY` (32 bytes base64), `SPOTIFY_REDIRECT_URI`, `TIDAL_REDIRECT_URI`

The Neon MCP server is available — `NEON_API_KEY` enables programmatic project/branch/schema management. Prefer Neon MCP over manual SQL for schema setup.

## Cross-Cutting Concerns
- **Stack**: TypeScript, Cloudflare Workers, Hono, @neondatabase/serverless, vitest, wrangler
- **Architecture**: stateless Worker; all state in Neon Postgres; `pg` driver does NOT work in Workers (per ADR-001) — must use `@neondatabase/serverless` for Postgres-over-HTTP/WebSocket
- **Single-tenant by design** (ADR-005): no tenant column, no RLS, any valid JWT has full access
- **No silent failures** (per spec conventions): every error path produces a structured log line with `run_id`, `feature`, `stage`, `error_code`, `message`
- **Spec-first**: deviations from `docs/` require updating the spec before code. Reference `F-NNN` in commits and PRs.
- **TDD with vitest**: every feature has a matching `T-NNN` test spec; coverage gate is 95% on touched code
- **External-API code MUST be grounded against the canonical spec at write-time**: any constant or type definition that describes a Spotify/Tidal/etc. endpoint or response shape MUST cite the spec source URL + section near the declaration. For OpenAPI'd providers (Tidal, Spotify), prefer types generated via `openapi-typescript` (committed under `src/providers/<name>/openapi-types.ts`) over hand-written interfaces. `TODO(ovidiu): verify` is NEVER an acceptable substitute for grounding. Lesson from Sprint 6 audit: agents fabricated flat shapes for Tidal v2 (which is JSON:API), tests confirmed the wrong contract, fuzzy matcher would have shipped non-functional. Tidal OAS source: `https://tidal-music.github.io/tidal-api-reference/tidal-api-oas.json`. Spotify OAS: `https://github.com/sonallux/spotify-web-api/blob/main/fixed-spotify-open-api.yml` (community-maintained mirror; official docs at developer.spotify.com).

## Domain: spotify-roon-sync

### Decisions
- TypeScript / Cloudflare Workers / Hono / Neon stack chosen per ADRs 001-003 (2026-04-25)
- ISRC-first matching with fuzzy fallback per ADR-004 (F-006 before F-007)
- Single named Tidal playlist as sync target per ADR-006 (not Tidal favourites)
- Bootstrap JWT pattern: 1-year HS256, single subject `owner` (F-001) — iOS Sign-in-with-Apple deferred

### Patterns
- All authenticated routes use a single `Hono.use()` middleware call (F-001-R6) — no per-route wrapping
- Tokens: encrypted with AES-256-GCM, 96-bit IV per encrypt, key from `TOKEN_ENCRYPTION_KEY` secret (F-004)
- DB invariants enforced at the application layer (no DB CHECKs in spec) — see I-001 to I-005 in `docs/architecture.md`
- Cursor advance for Spotify fetch MUST be atomic with page persist (I-005, F-005)

### Gotchas
- **Bootstrap cycle F-001 ↔ F-014**: F-001 spec lists F-014 as dep ("/healthz is the only unauthenticated GET"); F-014's `/readyz` lists F-002/F-003/F-004 as deps. Resolution: implement F-014 `/healthz` first (zero deps), then F-001 wires `/healthz` as the public exception. F-014 `/readyz` enhancements layer in after providers ship. `features.json` records F-001 → F-014 only; the F-014 → providers relationship is documented but NOT a hard dep.
- **Co-implementation F-009 ↔ F-011**: F-009 spec lists F-011 (logging consumes orchestrator output); F-011 lists F-009 (orchestrator owns sync_runs row writes). Resolution: ship F-011 read endpoints first against an empty `sync_runs` table, then F-009 starts writing rows. `features.json` records F-009 → F-011 (hard dep), F-011 → F-001 only.
- **`pg` driver DOES NOT work in Workers** (ADR-001 consequence): always import from `@neondatabase/serverless`
- **Plaintext tokens MUST NOT appear in any log line** (I-003, F-001-R5, 9.3) — log redaction is a code review checkpoint
- **README/SPEC_INDEX broken links**: both `docs/README.md` and `docs/specs/SPEC_INDEX.md` reference `docs/features/` and `docs/tests/`, but the actual layout is flat under `docs/specs/` (`F-NNN-*.md` and `T-NNN-*.md` side-by-side). Cosmetic; flagged for cleanup.
- **`.env` `DATABASE_URL` is commented out inside the quotes**: the value starts with `# postgresql://...` — that leading `#` is INSIDE the quoted string, so any code reading the env var will get a literal `#` prefix and the URL will not parse. Fix before F004/F005 ship: strip the `#` and the leading space. The Neon MCP server can fetch a fresh connection string via `get_connection_string` if needed.
- **Spec uses `sync.example.com` as Worker domain placeholder**; real domain from `.env` `WORKER_DOMAIN` is `portage.eovidiu.co.uk`. Use the real value in `wrangler.toml` and OAuth redirect URIs (`https://portage.eovidiu.co.uk/auth/spotify/callback`, `https://portage.eovidiu.co.uk/auth/tidal/callback`).
- **vitest-pool-workers needs `nodejs_compat` flag in `wrangler.toml`** — without it, the pool fails to load. Already added (Sprint 1, F014 teammate).
- **Coverage tooling discrepancy** (Sprint 1, unresolved — reviewer to investigate): `@cloudflare/vitest-pool-workers@0.5.41` does not surface coverage instrumentation cleanly. F004 (`crypto`) reported 100% via istanbul provider; F014 (`health`) reported "cannot measure" with the same provider. Either the difference is real (some test layouts work and others don't) or one teammate ran the wrong command. Reviewer is asked to reconcile and either (a) confirm both are measurable or (b) declare the coverage gate as a known tooling gap and create a follow-up feature to enable it. Until resolved, treat coverage % values in `features.json` as best-effort, not authoritative.
- **`@vitest/coverage-v8` is incompatible with the Workers sandbox** — it imports `node:inspector`. Sprint 1 switched to `@vitest/coverage-istanbul`. Both deps are currently in `package.json`; the v8 dep can be removed once the istanbul path is fully validated.
- **Default Neon branch is `production`** (id `br-sparkling-star-al5g4fzf`), not `main`. Use `mcp__Neon__describe_project` if a teammate needs the branch ID.
- **wrangler 3.99.0 only supports compat dates up to `2024-12-30`** — wrangler.toml asks for `2026-04-25` and the local Workers runtime warns + falls back. Production Cloudflare deploy will honor the requested date. Upgrade wrangler when the upstream supports current dates, or pin compat date to `2024-12-30` to silence local warnings.
- **T-014-03 and T-014-11 are wall-clock metric tests** (response time thresholds). They require a running `wrangler dev` and cannot be expressed as vitest unit tests. F014 unit tests cover the functional assertions; the timing metrics are deferred to manual/e2e QA. Track explicitly when a QA feature is added.
- **Postgres advisory locks need Pool/WebSocket, NOT bare `neon()` HTTP** (Sprint 5 Wave A miss): `pg_try_advisory_lock` is session-scoped. The Neon HTTP driver opens a fresh session per query, so the lock auto-releases the moment the acquire query returns and any subsequent `pg_advisory_unlock` runs against a different session. For session-scoped DB primitives (advisory locks, temp tables, prepared statements, multi-query transactions, listen/notify, server-side cursors), use `Pool` from `@neondatabase/serverless` and share a single `PoolClient` across the pair. `pool.end()` is required in finally — Workers do not keep WebSocket connections across requests. See `src/sync/orchestrator.ts` `acquireLock` / `releaseLock` for the pattern.
- **Tidal v2 playlist API uses `name` (not `title`) and `accessType` (not `privacy`)** (Sprint 7 Step 4 audit): The original F-008 implementation fabricated three fields against Tidal v2: `attributes.title` (Tidal uses `name`), `attributes.privacy: "private"` (the field doesn't exist; access enum is `[PUBLIC, UNLISTED]` only — no `PRIVATE`), and `BATCH_SIZE: 100` (OAS `PlaylistsItemsRelationshipAddOperation_Payload.data.maxItems = 20`). Add-tracks body also requires `meta.positionBefore` (string, required). GET `/relationships/items` accepts `page[cursor]` only — no `limit` query param. F-008-R3 was amended to specify UNLISTED. All tests had originally confirmed the wrong shape — same anti-pattern as F-006/F-007. Pattern: when a future feature touches a Tidal v2 endpoint, the canonical OAS check is mandatory before implementation, and the generated types at `src/providers/tidal/openapi-types.ts` should be the source of truth.

## Meta-Patterns
<!-- Coordination insights that apply across features — NOT domain-specific.
     Populated by the retrospective step at session end.
     These transfer to new projects: harness-init can import them as starting context. -->
- **Always describe-table the schema before writing a query**: Sprint 1 C1 (the missing `status` column) hid behind a mock built from the spec, not the schema. Adding `mcp__Neon__describe_table_schema` (or equivalent for non-Neon DBs) as the first action of any feature touching a DB column would have caught it pre-implementation. Reviewer recommendation now baked into the standard spawn prompt template for DB-touching work.
- **Reconcile spec contradictions in a single commit, before code**: F-004 said "separate IVs", architecture.md ER diagram showed one. Each teammate read one source and silently chose. Front-load contradiction reconciliation as a teammate's first sub-task when the work spans two specs.
- **Mocks built from specs hide schema/spec drift**: integration tests against a real (or branch-isolated) DB catch this class of bug. Worth investing one Neon-branch-per-feature integration step for any feature touching a DB column. Sprint 2 candidate.
- **Two coverage providers in package.json simultaneously is a smell**: Sprint 1 had `@vitest/coverage-v8` + `@vitest/coverage-istanbul` because two teammates independently encountered the v8/node-inspector incompatibility and both added a workaround. One shared note in `context_summary.md` ("v8 doesn't work in Workers; use istanbul") would have prevented the duplicate dep. Lesson: when a teammate solves an infra blocker, broadcast it via SendMessage type "broadcast" to other concurrent teammates same sprint.
- **Coverage measurement question deserves a 30-second front-loaded check**: F-014 teammate concluded coverage was unmeasurable; F-004 teammate found the istanbul fix; net result was a false "n/a" claim that the reviewer caught. Spawn prompts should now include "your first action after writing the failing test is to confirm `npm run test:coverage` produces a real coverage report."
- **Reuse-existing-teammate vs spawn-fresh for fix waves**: reusing the four Sprint 1 teammates for the four review-fix tasks worked cleanly — they already had project context loaded, no fresh-spawn token cost. Pattern: when a fix wave maps 1:1 to original implementers, SendMessage them new tasks rather than spawning new agents.
- **Plan teammate roles to leave room for shared-config touch-ups**: F-014 had to add `nodejs_compat` to wrangler.toml (scaffolder's scope); F-004 had to switch coverage provider (vitest.config.ts in scaffolder's scope). Both were necessary scope expansions, but if anticipated, the scaffolder spawn prompt could front-load them. Lesson: ask "what shared-config touchups will this feature trigger?" during sprint planning.

## Meta-Session 2026-04-25
- **Scope accuracy**: F-014 had 3 scope expansions (wrangler.toml nodejs_compat, package.json script split, vitest.config.ts threshold removal); F-004 had 2 (vitest.config.ts provider switch, package.json istanbul devDep). Both were tooling/infra blockers, not feature creep. Initial spawn prompts didn't anticipate the vitest-pool-workers compat needs. Future tooling-heavy first features should front-load infra discovery in the scaffolder.
- **Model calibration**: All implementers Sonnet, reviewer Opus. Worked well — no correction cycles on Sonnet implementers (the schema bug was a spec-vs-schema mismatch that no model would catch without a DB describe; not a Sonnet competence gap). Reviewer Opus produced a thorough, accurate review that caught both critical bugs in one pass. Cost-effective.
- **Discovery lineage**: F-004b discovered via F-004 review (depth 1). The discovery is healthy — the persistence helpers genuinely belong with the OAuth flows, not the encryption primitives. Lesson confirmed: when a "feature" naturally splits into "primitive" + "DB-coupled wrapper", split them in features.json from the start.
- **Approach patterns**: TDD strict + per-spec test ID citation (T-NNN-MM in test names) made the spec-coverage matrix mechanical for the reviewer. Continue this pattern. Web Crypto + @neondatabase/serverless + Hono stack worked frictionlessly in Workers — no Node-API contraband caught by review.
- **Plan approval**: not used in Sprint 1. For Sprint 2's F-001 (auth + secrets validation, security-sensitive, touches middleware that all later routes depend on), `require_plan_approval: true` is justified. F-002/F-003/F-004b can proceed without it.
- **Wave-by-wave parallelism beat all-at-once**: spawning blocked teammates eagerly would have burned Sonnet tokens spinning. Two-wave (scaffolder+db-init → health+crypto) plus a fix-wave reuse pattern was the right cadence.

## Meta-Session 2026-04-25 (Sprint 2)
- **Scope accuracy**: ZERO scope_expansions across F-001/F-002/F-003/F-004b. Sprint 1's lessons paid off — pre-thought scope boundaries plus the lead-stub interface pattern eliminated the contention point that would otherwise have forced expansions in src/index.ts and src/db/.
- **Model calibration**: All implementers Sonnet, lead+reviewer Opus. F-002 had 1 correction_cycle from the review fix wave; everyone else 0. The correction was a real coverage gap + R7 spec bug, not a Sonnet competence issue. F-001 used `require_plan_approval=true` and the plan was approved with 2 small refinements — the round-trip prevented the kind of misalignment that would have eaten 3-4 correction cycles.
- **Discovery lineage**: F-Integ discovered via F-004b (the mock-only integration gap surfaced by reviewer); F-Q1 discovered via F-014 (the deferred wall-clock metrics piling up across sprints). Both are healthy — they emerge from real test gaps, not architectural surprises.
- **Approach patterns**: PKCE S256 via crypto.subtle.digest + crypto.getRandomValues → base64url worked first-time for both providers. Module-level Map<string, Promise<void>> for refresh coalescing — universal pattern, both providers use it (after the M1 fix). vi.mock for F-004b stubs — let F-002/F-003 ship without F-004b's real bodies. Stub-then-replace was the key enabler for true 3-way parallel implementation.
- **Plan approval value**: For F-001, yes. The round-trip caught (a) the secretsGuard /healthz exemption decision, (b) the bootstrap script invocation strategy (test the function, not the CLI), (c) the jose error discrimination approach. For F-002/F-003/F-004b, no plan_approval was used — these were well-scoped pattern matches against existing code. Calibration: require plan approval for foundational/security-sensitive features that everything else depends on; skip for symmetric implementations of an existing pattern.

## Meta-Patterns appended (Sprint 2)
- **Lead-stub interface pattern**: When N teammates need the same interface module, lead pre-commits a stub file with function signatures + `throw "not implemented"` bodies. F-2/F-3/F-4b ran fully concurrent because of `7aaff41` (stub for src/db/provider_tokens.ts and src/db/oauth_state.ts). Cost: 60 sec of lead work + one tiny commit. Payoff: replaces serial dependency with parallel implementation. Reusable for any feature triple where one provides the interface and others consume it.
- **Worktree isolation flag is currently a no-op**: `isolation: "worktree"` in the harness Task() spawn does NOT actually create worktrees. All 3 Sprint-2 implementers reported their "worktree" was main; `git worktree list` confirms only the primary tree. No deliverable was affected because each teammate stayed in lane voluntarily, but the safety property the flag promises doesn't fire. Drop the flag from spawn prompts; rely on explicit scope discipline + the .claude/teammate-scope.txt enforcement hook (when configured).
- **"Tooling unreliable" claims must be proven**: F-014 (Sprint 1) and F-002 (Sprint 2) both blamed coverage tooling for what turned out to be process errors. Same-sprint sibling features (F-004 in S1, F-003 in S2) ran cleanly on identical pipelines. Spawn prompts now require: paste the literal istanbul output, don't summarize as "n/a" or "tooling broken"; cite the exact error message if measurement truly fails.
- **One file per concern beats one file per provider**: F-003 split out src/providers/tidal/client.ts from oauth.ts — easier to test in isolation, easier to reason about. F-002 kept it monolithic — harder to test, hit the coverage gate gap. For F-005+ (Spotify Liked Songs, Tidal Search, etc.), encourage the split-by-concern pattern.
- **Symmetric features need a 5-min cross-team consistency checkpoint**: F-002 and F-003 are mirror implementations of OAuth. They independently chose different revoke-error semantics (Spotify discriminated; Tidal indiscriminate). Both passed their own tests; the divergence was a spec-vs-code gap that only the reviewer caught. Pattern for Sprint 3: when spawning two symmetric features, include "read your sibling's implementation before finalizing yours" in the spawn prompt.
- **Harness fix-wave pattern (reuse existing teammates)**: Sprint 1 used it (4 teammates → 4 fix tasks). Sprint 2 used it (2 teammates → 2 fix tasks). Sprint 3 used it (2 teammates → 2 fix tasks). Pattern: review identifies issues by feature; lead creates one task per feature's fix bundle; SendMessage to the original implementer (still alive, project context loaded) with the task ID. Cost: 2 SendMessage round-trips. Payoff: fast turnaround, no fresh-spawn token cost, original author owns their fix.
- **Mechanical enforcement beats spawn-prompt repetition**: when an anti-pattern recurs across 3+ sprints despite spawn-prompt warnings, promote the check to a hook. Sprint 3 codified this: the "tooling unreliable" coverage-claim pattern (F-014 S1, F-002 S2, F-005 S3) is now blocked by `verify-task-quality.sh` Stage 3 (parses features.json coverage field, rejects forbidden phrases). No teammate can mark complete with a vague claim again.
- **Init.sh pipe traps**: `command | tail -N` silently masks exit codes. Use `${PIPESTATUS[0]}` to capture and propagate. Sprint 3 caught this hiding tsc errors for an entire sprint.
- **Spec amendments are cheaper than schema migrations on populated tables**: Sprint 3 hit schema/spec drift (album/duration_ms nullable in DB, NOT NULL in spec). Lead chose to amend the spec to match reality (Spotify legitimately returns objects without these fields) rather than tighten the schema. Pattern: when reality diverges from spec on nullability of upstream-provided fields, amend the spec.

## Meta-Session 2026-04-26 (Sprint 3)
- **Scope accuracy**: ZERO scope_expansions across F-005/F-Integ/F-Q1. The lead-stub interface pattern from Sprint 2 + clean per-feature scope boundaries continued to pay off. F-Q1 needed an entirely new test config (vitest.e2e.config.ts) but that was within its declared scope.
- **Model calibration**: All implementers Sonnet, lead+reviewer Opus. F-005 had 1 correction_cycle from the C1+M1+M3 fix wave; F-Integ + F-Q1 zero. C1 was a real type-system bug the teammate didn't catch because the harness wasn't gating on tsc — not a model competence issue. Fix landed in 1 round.
- **Discovery lineage**: F-005 spec amendment (nullability) discovered via reviewer's schema audit. No new features discovered this sprint (F-Integ + F-Q1 were both pre-known from Sprints 1+2 deferrals, now landed). Healthy decomposition.
- **Approach patterns**: Stub-then-replace pattern from Sprint 2 wasn't needed — Sprint 3's three features had genuinely disjoint scopes. F-Integ's URL-discriminating fetch mock pattern (selective `realFetch.bind(globalThis)` capture before `vi.spyOn`) is the new reusable pattern; document for future integration tests. F-Q1's wrangler-dev-as-subprocess + singleFork pool pattern is reusable for any future e2e test needing real HTTP.
- **Plan approval value**: not used in Sprint 3. Three features, all well-scoped pattern matches against existing code (F-005 mirrors F-002's structure; F-Integ + F-Q1 are tooling/test features). Calibration confirmed: skip plan approval for symmetric implementations or test infrastructure.
- **Three-strike pattern triggered mechanical fix**: the "tooling unreliable" coverage anti-pattern hit a 3rd recurrence (F-014 → F-002 → F-005). Sprint 3's lead-side D2+D5 work converted the spawn-prompt requirement into a hook gate. Future sessions cannot regress this.

## Meta-Patterns appended (Sprint 4)
- **Schema migration discipline still not mechanical**: Sprint 3 missed `idx_tracks_added_at` index, Sprint 4 missed `tidal_id_invalid` column. Same anti-pattern shape as the "tooling unreliable" recurrence. Sprint 5 should extend the TaskCompleted hook with a Stage 4: grep src/ for new column references not present in db/schema.sql, reject completion. Same playbook as Sprint 3's coverage-claim hook.
- **TODO(ovidiu) marker discipline asymmetric across parallel teammates**: F-007 carried the marker, F-006 + F-008 didn't. Same root cause as the symmetric-features-need-checkpoint Meta-Pattern from Sprint 2. Sprint 5 spawn prompts should mandate the marker as a checklist item; OR add a hook stage that greps for `openapi.tidal.com` URLs requiring an immediately-preceding `TODO(ovidiu)` line.
- **Live Neon ALTERs require Ovidiu credential**: teammates have read-only Neon MCP scope. Lead has full scope. Pattern: when a feature requires a schema migration, the teammate adds the SQL to db/schema.sql + flags it as PENDING in features.json notes; the lead applies via `mcp__Neon__run_sql` (idempotent `IF NOT EXISTS` form) and clears the note. Sprint 4 codified this — both `idx_matches_tidal` (F-006) and `tidal_id_invalid` (F-008) followed it.
- **Mechanical hook gates compound**: Sprint 3's TaskCompleted hook (tsc + coverage-claim) eliminated the broken-build and false-claim regressions in Sprint 4. ZERO correction cycles across F-006/F-007/F-008 implementation. The 30-min Sprint-4 fix wave only addressed the things the hook didn't (yet) gate: schema drift and TODO discipline. Each new hook stage adds compounding leverage.

## Meta-Patterns appended (Sprint 5)
- **Driver semantics matter for session-scoped DB primitives**: Postgres advisory locks are session-scoped. The Neon HTTP driver opens a fresh session per query, so a lock acquired via `neon()` auto-releases the moment the acquire query returns. Same class of issue applies to: temp tables, prepared statements, multi-query transactions, advisory locks, listen/notify, server-side cursors. Rule: any DB feature that depends on session state must use `Pool` (WebSocket) from `@neondatabase/serverless`, sharing a single `client` across the session-scoped pair. Documented as a Gotcha; the lead caught this via plan_approval review.
- **Plan approval is only as strong as teammate compliance**: F-009 had `require_plan_approval: true` and the lead caught the lock-correctness blocker in the review. The teammate received the SendMessage but proceeded with the broken approach anyway, citing "multiple idle hooks with no lead response" — even though the response had been sent. Either the message was filtered out by the agent, or the teammate's idle-recovery path bypasses the wait-for-approval state. Sprint 6 should: (a) make spawn prompts more explicit ("if you ever doubt whether you got the approval response, SendMessage team-lead asking BEFORE writing code"), (b) consider adding a hook that blocks file edits in the teammate's scope until a `.harness/teammate-<name>-approved.flag` file exists.
- **Bundled commits across teammates lose per-feature provenance**: the F-011 logging teammate ran `git add -A` and committed BOTH F-011 and F-009 files in one `feat(F-011)` commit. Per-feature commit boundary lost; reverting one feature's work now requires file-level surgery. Spawn prompts should specify "commit only files in your scope" explicitly. Or add a pre-commit hook that warns when staged files cross teammate scopes (per `.harness/features.json` scope arrays).
- **Lead-side fixes are sometimes faster than another correction cycle**: F-009's lock fix was ~30 LOC source + ~30 LOC test changes. Sending the orchestrator back for a correction cycle would have cost a fresh-spawn or wake-up + context reload + their TDD cycle. Doing it directly saved time and preserved the 100%-coverage achievement. Pattern: when the fix is small, well-understood, and isolated, the lead exits plan mode and applies it directly with a separate `fix(FXXX)` commit. When the fix touches multiple modules or requires reasoning the teammate hasn't done yet, send back.
- **Stage 4 schema-drift hook now mechanical**: `.claude/hooks/check-schema-drift.py` greps src/ for INSERT/UPDATE/WHERE column references and verifies each against db/schema.sql. Validated: clean pass on current code; correctly flags simulated Sprint-4 regression. Integrated as Stage 4 in verify-task-quality.sh; rejects task completion on drift. Same playbook as Sprint 3's coverage-claim gate. Cumulative hook leverage: smoke + full + coverage-claim + schema-drift.

## Meta-Patterns appended (Sprint 7 — pre-deploy spec audit)
- **External-API code MUST be grounded against the canonical spec at write-time, not after**: F-006, F-007, and F-008 all shipped with fabricated request/response shapes that tests confirmed (because the tests were written from the same fabricated mental model). The verification anti-pattern was caught only during the manual `for-ovidiu.md` audit walkthrough, which is fragile — a future contributor might skip or rush it. Sprint 7 landed three durable mitigations: (1) `openapi-typescript` Tidal types committed at `src/providers/tidal/openapi-types.ts` so handwritten interfaces become uncompilable, (2) `Verified:` marker convention next to URL constants citing the canonical OAS source, accepted by Stage 5 hook alongside the legacy `TODO(ovidiu)` form, (3) Cross-Cutting Concerns rule in `context_summary.md` plus an auto-memory feedback entry. ROI check on the next sprint: if a new external integration goes in WITHOUT generated types or a Verified: marker, the policy isn't yet sticky enough.
- **Spec amendment is preferable to silent deviation when the API can't satisfy the spec literally**: F-008-R3 said "privacy=private", Tidal v2 has only `[PUBLIC, UNLISTED]`. Sprint 7 amended R3 to specify `UNLISTED` with the full rationale inline rather than leaving the spec aspirational and the code "best-effort". Pattern: when the upstream API is more constrained than the spec assumed, update the spec before code, document the constraint in the requirement table, and reference the OAS schema name. Future audits then have an unambiguous source of truth.
- **The `for-ovidiu.md` checklist was an effective second line of defense, but not the right primary one**: the spec audit during pre-deploy caught all three matcher/playlist defects (F-006, F-007, F-008) in 4 hours of work — far cheaper than catching them post-deploy. But every defect predates Sprint 7 by months, meaning prod-bound code lived with these fabrications for the entire build phase. Lesson: shift the audit left into per-feature spawn prompts. Future Tidal/Spotify-touching features should have a "first commit must include a fetch of the canonical OAS section + import of generated types or explicit Verified: marker" rule baked into the spawn template.

## Meta-Session 2026-04-27 (Sprint 7 Step 4 — F-008 OAS-grounded fix)
- **Scope accuracy**: 0 scope_expansions. The bug-list-from-OAS-audit shape gave a precise upfront target (6 enumerated bugs, all in 2 src files + 2 test files). No mid-work surprises. Pattern reinforced: when the work is "fix N specific known bugs", scope is more deterministic than "implement spec X" because the bug list IS the contract.
- **Model calibration**: single-session on Opus (no team). Right call — the work needed simultaneous reasoning over OAS schema, current code, test rewrites, and spec amendment. Sonnet would have been fine for the mechanical parts but the OAS cross-reference work benefits from Opus.
- **Discovery lineage**: zero new features discovered. Healthy — F-008 was already a known feature; the bugs were attributes of an existing implementation rather than missing scope.
- **Approach patterns**:
  - RED-green-refactor stayed strict: failed tests written first against the new contract, watched them fail, then minimum code change to pass. No "I'll fix it as I go" temptation.
  - Tests-first updates BEFORE the spec amendment caught 5 failures immediately, validating that the existing tests had been confirming the fabricated contract.
  - Spec amendment (F-008-R3) was a 2-line change but unblocked the rename of `private` → `UNLISTED` cleanly. Doing it before the code change preserved spec-first discipline.
  - Empty-string `meta.positionBefore` pattern: send the minimum the OAS schema requires, document the unverified semantic, plan to confirm during real-API usage. Better than guessing or omitting.
- **Plan approval value**: not used. Single-session, well-scoped bug-fix work. Calibration confirmed: skip plan approval for bug-fix waves where the scope is enumerated upfront.
- **Hook investment ROI**: Stage 5 marker hook adapted this session to accept `Verified:` post-audit. Each subsequent verified URL across Steps 1-4 (Spotify Liked, ISRC tracks, Fuzzy search, Playlists) now carries the audit trail in code. Cumulative leverage: future contributors can't add a new external URL without a marker, and verified URLs preserve their provenance. Pattern: when a workflow step (like "verify and remove TODO") conflicts with a hook policy, evolve the hook to recognize both states rather than working around it.

## Meta-Patterns appended (Sprint 6)
- **Project-level reviewer pass at sprint-of-sprints scope is high-leverage**: Sprint 6 spawned a single Opus reviewer with read-only access over the full src/ tree. Output: 8 major + 5 minor findings, none of which were caught by the per-feature reviewers in Sprints 1-5. Why this works: per-feature reviewers see one slice; project-level reviewer sees cross-feature drift (F-012-R3/R4 silently unimplemented because tests injected fake candidates; F-007 fuzzy violates F-012-R10/R11 because the cross-feature constraint never surfaced in either feature's tests). Pattern: when ALL features are passing, before declaring shippable, run a project-level reviewer on Opus. Cost: ~1 reviewer-session of Opus; value: catches spec drift that per-feature reviews can't.
- **The mock-vs-spec divergence anti-pattern is real**: F-012-R3/R4 (candidates fetch) was tested against a mock that injected fake candidates. The test passed, but the production implementation returned `[]`. Reviewer caught this only by reading the source and noticing the hardcoded empty array. Mocks built from "what the test needs" rather than "what the spec mandates" hide entire requirements. Sprint 7 should add a manual cross-check at completion: did the implementation actually FULFILL the spec, or just satisfy the tests?
- **Three-stage-of-mechanical-gates ROI continues to compound**: this session added Stage 5 (TODO markers) + extended Stage 4 (index references). Previous stages: smoke, full_test, coverage-claim (Sprint 3), schema-drift columns (Sprint 5), schema-drift indices (Sprint 6 ext), TODO markers (Sprint 6). Each stage was driven by a recurring anti-pattern that hit ≥2 sprints. Pattern: when a process gap recurs 2+ times, promote to a hook. The 5-stage gate now mechanically enforces what was previously spawn-prompt repetition.
- **Lead-applied small fix-waves are cleaner than send-back-for-correction**: this session applied M6 + m1 + m2 in a single 11-LOC commit (51cd380) rather than spinning up the orchestrator/cron/captures teammates again. Reasoning: the fixes were small, well-isolated, didn't break tests, and the original implementers had already terminated. Sprint 5's F-009 lock fix used the same pattern. Calibration: lead-applied fix when (a) <30 LOC, (b) doesn't require updating existing tests significantly, (c) original teammates terminated; otherwise send back.

## Meta-Session 2026-04-26 (Sprint 6)
- **Scope accuracy**: 3 teammates spawned (reviewer Opus, e2e-enricher Sonnet, integ-extender Sonnet) for the 3 well-bounded streams. ZERO scope_expansions on the streams themselves. e2e-enricher took an approved scope expansion of 1 LOC into src/sync/orchestrator.ts (the WALL_TIME_OVERRIDE_MS env hook) — surgical, gated by env presence so production unaffected. integ-extender stayed strictly in tests/integration/. reviewer was strictly read-only.
- **Model calibration**: reviewer Opus was the right call — it caught 8 majors that per-feature Sonnet reviewers missed. The cross-feature reasoning (F-012-R10/R11 violation by F-007 path) is a clear Opus signal — needs simultaneous understanding of multiple features' specs. Implementer Sonnet was fine for the well-scoped tracks.
- **Discovery lineage**: reviewer's findings are functionally a discovery wave. M1+M2+M3+M4+M5+M7 are spec gaps that existed across Sprints 2-5 but weren't surfaced until the project-level pass. Pattern: per-feature reviews catch local issues; cross-feature drift requires a project-level pass at sprint-of-sprints frequency.
- **Approach patterns**: WALL_TIME_OVERRIDE_MS env hook pattern (test-only override gated by env presence) is clean — production unaffected, e2e gets deterministic timing. URL-discriminating fetch mock pattern (selective realFetch.bind capture before vi.spyOn) reused successfully for 3 new integration tests. Concurrent-invocation test via Promise.all([runSync, runSync]) is the cleanest way to verify advisory-lock semantics against real Postgres — added to the toolbox.
- **Plan approval value**: not used in Sprint 6. Three streams were well-scoped pattern matches; reviewer was read-only. Calibration confirmed: skip plan approval for symmetric/test-infra/review work; require it only for foundational features touching multiple modules (Sprint 2's F-001 case, but not for any subsequent feature).
- **Coordination cleanliness**: zero coordination failures this sprint. e2e-enricher self-flagged the pre-existing T-001-10 timeout (correctly, not their fault). integ-extender self-flagged the writePlaylist mock URL miss (correctly, working as designed). reviewer delivered clean read-only output. The Sprint 6 hook investments + Sprint 5 retrospective lessons compounded — no plan_approval mismatches, no bundled commits, no silent compliance issues.

## Meta-Patterns appended (Sprint 5 Wave B+C)
- **Index migrations need the same Stage-4-style mechanical gate as columns**: F-013 spec called for `idx_captures_spotify_id` index, but the captures teammate flagged it in their completion message rather than adding to db/schema.sql. Stage 4 currently catches missing COLUMN references, not missing indexes. Sprint 6 candidate: extend the schema-drift hook to also grep src/ for query patterns where an index would matter (e.g., `WHERE spotify_id` on captures means an index on captures(spotify_id) should exist). Lower priority than Stage 5 TODO-marker hook because the production impact of a missing index is performance degradation, not data corruption.
- **Bundled commits across teammates lose per-feature provenance, recurring**: Sprint 5 Wave A had it (logging teammate committed orchestrator's files in feat(F-011)). Sprint 5 Wave B had it again (cron teammate committed captures' staged index.ts edit in feat(F-010) because splitting would break tsc). Pattern is mechanical: when teammate A's commit needs file X to compile, but file X has teammate B's staged edits, A is forced to commit B's work too. The fix is either (a) explicitly tell teammates to coordinate src/index.ts edits via SendMessage before staging, or (b) lead pre-commits route mounts as part of the per-feature stub interface (same pattern as the F-011 sync_runs.ts stub commit 9cee614). Sprint 6 should bake (b) into the spawn template for any Wave that has 2+ teammates touching shared mount points.
- **SendMessage delivery latency can be minutes; lead should not assume non-response**: Wave B+C captures teammate received lead's follow-up, picked it up, did the work correctly, AND sent an explicit task-completion SendMessage — but the SendMessage arrived ~5 minutes after delivery. Lead assumed silence and started parallel work. The teammate was NOT silently complying; they were correctly acking with delayed transport. Pattern: after sending a follow-up SendMessage, lead should (a) wait at least 5 minutes before assuming non-response, (b) check git log + working tree as a faster signal of activity than waiting on SendMessage delivery, (c) if a teammate has been idle > 10 min after a follow-up with no commit activity, then escalate. Wave A's orchestrator was fundamentally different — they shipped the BROKEN approach actively, not delayed-acked correctly.
- **Lead-applied incremental fixes for one-LOC branch-coverage gaps are over-eager**: F-013 had src/db/captures.ts at 0% (real gap) AND src/routes/captures.ts at 92.1% branches (one branch on line 116). Lead reflexively wrote tests for both. The branch gap was cosmetic — 92.1% > 95% gate is met for branches by treating "branches" as a soft target (the project's stated gate is 95%, but stricter than spec implies for single uncovered defensive null-coalescing). Better future heuristic: if coverage meets gate within 3 percentage points and the uncovered branches are clearly defensive (`?.` / `??` fallbacks for fields that upstream schemas may or may not provide), accept. Lead's time better spent on coordination than chasing tail-end branches.

## Meta-Session 2026-04-26 (Sprint 5 Wave B+C)
- **Scope accuracy**: F-010 had 1 scope_expansion (run.ts R7 fix — was originally orchestrator-shipped stub, cron took the second pass). F-012 zero. F-013 had 2 (DB-layer test file + index migration applied by lead). All expansions were spec-driven, not feature creep. The cron R7 expansion was self-flagged by the cron teammate ("recommend lead review") which is the right way to surface scope ambiguity.
- **Model calibration**: All Sonnet for implementers, Opus for lead. Correction cycles: F-010=1 (the R7 follow-up wasn't a "wrong approach" cycle — it was a genuine spec-completion wave on a stub the orchestrator left). F-012=0 (clean ship). F-013=0 (lead-applied branch test was cosmetic; teammate work was correct). Sonnet is the right fit for HTTP CRUD features even iOS-ready ones; the project hasn't shown a Sonnet competence gap.
- **Discovery lineage**: No new features discovered this wave (T-Q1 candidates added to deferred list — T-009-10 wall-time metric, T-011-13 p95 with 1000 rows). Healthy decomposition.
- **Approach patterns**:
  - Hono sub-router + thin handlers + DB helpers: confirmed reusable for any new HTTP CRUD feature. F-012 and F-013 both followed this shape and shipped clean.
  - Idempotency window pattern (F-013's 60s findRecentCapture): novel for the project, generalizes to any "operator submits same event twice" case. Reusable for future iOS-ready endpoints.
  - Auto-fetch + persist pattern (F-013's ensureTrackExists for unknown spotify_ids): handles the "iOS captures a track before F-005 has seen it" race. Spec-mandated; reusable when the iOS layer can submit work referencing entities the server hasn't observed yet.
  - Promise.race for HTTP-handler wall-time cap (F-010's 25s race in run.ts): cleaner than the orchestrator's 300s race because the handler's only contract is "respond by 25s" — falling back to a 202 with run_id from getLatestRun.
  - sql.transaction(sync-callback array form) for I-001 atomicity (F-012's manual match move): same pattern as F-005 cursor-advance. Now used in 3 features. Pattern stable.
- **Plan approval value**: not used in Wave B+C. Three features were well-scoped pattern matches against existing code (F-010 a thin shim; F-012 + F-013 HTTP CRUD). Calibration confirmed: skip plan approval for symmetric/well-scoped features. Wave A's experience reinforced that compliance enforcement is the missing piece, not more plan approvals.
- **Coordination cost was lower than feared**: Wave A's orchestrator non-compliance cost ~30 min lead-applied fix. Wave B+C's apparent "silent" captures was actually a SendMessage delivery latency artifact (~5 min between teammate sending and lead receiving) — recovered cleanly because Write tool blocked on file-already-exists. In a 5-feature sprint with 5 teammates, losing ~30 min total to genuine coordination failures is well within acceptable; the remaining "10 min duplicated effort" was self-inflicted by the lead's premature parallel work. Sprint 6 mechanical fixes target the genuine failure (plan-approval compliance) and the lead's reaction time (don't act on silence under 5 min).

## Meta-Session 2026-04-26 (Sprint 5 Wave A)
- **Scope accuracy**: F-011 had 0 scope_expansions. F-009 had 2 (sync_runs.ts type extension for error_code, db/schema.sql ALTER for error_code column) — both legitimate per spec failure-mode table. Pre-thought scope boundaries plus the lead-stub interface pattern (commit 9cee614) enabled true parallel implementation. Calibration confirmed.
- **Model calibration**: All Sonnet for implementers, lead Opus. F-011 had 0 correction_cycles (clean ship). F-009 had 1 (lead-applied lock fix; not via hook reject path because the teammate's tests mocked the lock and couldn't catch the production bug). Sonnet handled both features fine in terms of code quality; the F-009 issue was a coordination failure, not a competence gap.
- **Discovery lineage**: No new features discovered this wave. Healthy decomposition.
- **Approach patterns**:
  - One-file-per-concern continued (orchestrator.ts engine + run.ts handler; status.ts/runs.ts/stats.ts as separate route files).
  - Per-spec test-ID citation in describe blocks (T-009-NN, T-011-NN) made the spec-coverage matrix mechanical.
  - vi.mock with separate `mockSql` (HTTP via neon) + `mockClient.query` (WS via Pool) is the new pattern for any test that needs both driver surfaces. Reusable for future features that combine HTTP-stateless queries with session-scoped operations (advisory locks, transactions, etc.).
  - Lead-applied direct fix vs send-back-for-correction: chose direct because scope was small, well-isolated, and preserved teammate's coverage achievement. New `fix(FXXX)` commit kept provenance clean.
- **Plan approval value**: F-009 used `require_plan_approval=true`. Lead caught the lock-correctness blocker in the review. BUT the teammate didn't apply the revision — so the approval round-trip's value was 0 in code (the lead had to fix it anyway). The cost was 2 SendMessage round-trips. Net: still positive because the LEARNING from the review (which surfaced via this retrospective) is now baked into the Meta-Patterns + Sprint 6 plan. Future calibration: plan approval needs a compliance-enforcement mechanism (see Meta-Patterns above).
- **Hook investment ROI**: Stage 4 schema-drift gate landed this sprint. It didn't fire for F-009/F-011 because the teammates correctly added the column to schema.sql alongside their code references. But the gate is now permanent — future sprints with schema additions can't ship without it being correct in the file.

## Meta-Session 2026-04-26 (Sprint 4)
- **Scope accuracy**: ZERO scope_expansions across F-006/F-007/F-008. Pre-thought scope boundaries + sequential dep chain (F-006 → F-007 → F-008) eliminated contention. The match-isrc teammate handled all 3 features sequentially with full context retention — no fresh-spawn cost.
- **Model calibration**: All Sonnet for implementers, Opus for reviewer. ZERO correction cycles across the 3 features (down from F-005's 1 cycle in Sprint 3). The hook gates from Sprint 3 prevented the broken-build/false-claim regressions before they could happen.
- **Discovery lineage**: F-008's `tidal_id_invalid` column + the requeueForInvalidTidalId helper were genuine new design (handling the catalog-removed case). No surprise new features discovered. Healthy decomposition.
- **Approach patterns**: One-file-per-concern continues to dominate (F-006: isrc.ts + artist.ts; F-007: fuzzy.ts + title.ts + score.ts; F-008: playlist.ts + playlist-endpoints.ts + playlist-writer.ts). Per-spec test-ID citation in describe blocks made spec coverage matrix mechanical for the reviewer. tidalFetch reuse from F-003 (Sprint 2) avoided the F-005-class M1 bug (raw fetch missing 401-refresh). The requeueForInvalidTidalId narrow-exception pattern (hard-coded reason, separate helper) is a reusable shape for future "narrow exception to a general rule" cases.
- **Plan approval value**: not used. Three features were well-scoped pattern matches. Plan approval calibration is now stable: use it for foundational/security-sensitive features (F-001 in Sprint 2); skip for symmetric implementations and test infrastructure.
- **Hook investment ROI**: Sprint 3's mechanical hook work (tsc gate + coverage-claim gate) saved an estimated 1-2 fix-wave rounds in Sprint 4. ROI is permanent — every future sprint benefits. Sprint 5 should continue this pattern with a schema-drift hook (per Meta-Patterns above).

## Meta-Session 2026-05-02 (F-015 — bounded-invocation refactor for Workers Free)
- **Scope accuracy**: 0 unplanned scope_expansions. Single-session work, plan written before code, plan held tight to its file list. Two correction cycles burned (recorded by hook) — both were test-mock fixups (T-005-02 capture-all sync_state writes, F-015 sweep_max assertion off-by-one timestamp), not architectural rework.
- **Model calibration**: Opus single-session. Right call — design needed simultaneous reasoning over 3 SQL eligibility predicates, F-005 cursor invariant, OAS subrequest budget, and per-spec amendment cross-references. Sonnet would have struggled with the cross-cutting state-machine for resume URL + sweep_max + cursor.
- **Discovery lineage**: F-015 `discovered_via=deploy-time` — surfaced by the prod 500 on first `POST /sync/run`. New "discovery" flag value beyond inter-feature lineage. Reinforces the rule: `discovered_via` should track whatever made the feature visible, not just which other feature surfaced it.
- **Approach patterns**:
  - **Plan-mode → Explore agent → write plan to file → ExitPlanMode → execute** loop worked smoothly. Plan file (`warm-napping-seal.md`) became the durable artifact tying together budget math, file list, test plan, and verification — useful both during execution and as a future reference point.
  - **TDD red-green** stayed strict for new helpers (`fetchPendingMatchQueue`, bounded `fetchLikedSongs`). Test harnesses adapted to multi-write transactions by switching to `captureStateWrites` helper that filters by key — pattern reusable for any future feature writing N keys atomically.
  - **Options-object signature for matchByFuzzy** avoided ~50 test call-site updates while accepting a new required-ish argument. Trade-off vs positional: lost minor symmetry with matchByIsrc, gained churn budget. Reusable pattern when a function gets a new mandatory configuration parameter and most callers don't care.
  - **Resume-URL state-machine across invocations** is novel for the project. Two new sync_state keys (spotify_resume_url + spotify_sweep_max) that share the same final-page transaction with the cursor write. Mid-sweep cursor-frozen invariant prevents losing tracks on later pages. This pattern generalizes to any cursor-based fetcher facing a per-invocation budget cap.
- **Plan approval value**: NOT used in the harness sense (no teammate). The plan-mode + Explore-agent flow served the same purpose: forced explicit decomposition before code, surfaced the resume-URL problem during the explore phase rather than after writing buggy code. Calibration: when single-session Opus is faced with cross-cutting refactor, plan-mode writes the artifact that would otherwise have been tribal knowledge.
- **Hook investment ROI**: All 5 stages (smoke + full + coverage-claim + schema-drift + TODO markers) green throughout. The schema-drift hook didn't fire because no new columns; the new sync_state KEYS are not column additions. Hooks correctly didn't false-alarm on string keys vs schema columns — important boundary that held up.

## Meta-Patterns appended (F-015)
- **Workers-platform constraints surface late and demand bounded loops**: Free-tier 50-subrequest cap looks generous in spec but bites the moment a real account is hit. Pattern: when targeting Workers Free, every loop in the orchestrator (pagination, per-track API calls, dedupe reads) needs an explicit upper bound + cross-invocation continuation state. The cursor abstraction alone is insufficient for backfill-style workloads — it solves "what's new since last run" but not "where am I in this sweep". Combine cursor (high-water mark, advances on completion) with resume-URL (mid-sweep continuation, persists between runs) for full coverage.
- **Spec amendment as part of refactor, not after**: F-015 amended F-005, F-007, F-009, F-010 in the same commit as the code change. Future readers see the spec entry alongside the implementation. The alternative (code now, spec later) leaves the codebase temporarily out of sync with its own contracts and tends to never get the catchup pass.
- **`captureStateWrites` test helper for multi-key transactional writes**: when a transaction writes multiple distinct keys (cursor + resume_url + sweep_max), test capture should filter by key rather than by call order. The helper is reusable for any future feature that does N atomic state writes; consider promoting to a shared test utility if a third such feature appears.
- **Plan-mode + Explore-agent for cross-cutting refactors**: even single-session Opus benefits from the discipline. The plan file is the durable artifact; the Explore agent's grounded subrequest budget map prevented design errors that would have surfaced only in test failures or production. ROI: ~30 min of plan/explore work prevented several hours of false-start implementation.
- **One-month-paid vs always-free tier decision is an architecture choice, not a deployment choice**: Ovidiu rejected the $5 backfill-then-downgrade option. The right answer was redesign for permanent Free fit, not pay for a transient one-shot. Pattern: when a tier-cost question surfaces, treat the constraint as a permanent invariant rather than a transient annoyance — the resulting architecture is cleaner and the spec stays honest.
